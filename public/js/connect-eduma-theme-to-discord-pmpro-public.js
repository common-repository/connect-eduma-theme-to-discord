(function( $ ) {
	'use strict';
/* 
console.log(etsEdumaPmproParams);

$('#eduma-pmpro-disconnect-discord').on('click', function (e) {
	e.preventDefault();
	var userId = $(this).data('user-id');

	$.ajax({
		type: "POST",
		dataType: "JSON",
		url: etsEdumaPmproParams.admin_ajax,
		data: { 'action': 'eduma_pmpro_disconnect_from_discord', 'user_id': userId, 'ets_eduma_pmpro_discord_nonce': etsEdumaPmproParams.ets_eduma_pmpro_discord_nonce, },
		beforeSend: function () {
			$(".ets-spinner").addClass("ets-is-active");
		},
		success: function (response) {
			console.log(response);
			return;
			if (response.status == 1) {
				window.location = window.location.href.split("?")[0];
			}
		},
		error: function (response) {
			console.error(response);
		}
	});
}); */

})( jQuery );
